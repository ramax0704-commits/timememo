# PRD: 타임메모 (Time Memo)

## 1. 한 줄 정의

하루를 시간 단위로 적어두지만 다시 들여다보지 않는 사람이, **적는 것 말고는 아무 일도 하지 않아도 자기 하루가 정리되도록**, 메모에서 활동과 지출을 자동으로 뽑아내는 AI를 얹은 **모바일 웹앱**

## 2. 문제와 타깃

- **문제**: 하루를 기록하는 것까지는 되는데, 그 기록이 쌓이기만 하고 아무것도 되지 않는다. 습관을 세려면 키워드를 미리 등록해야 하고, 지출을 보려면 금액을 정해진 모양으로 적어야 한다. 기록하는 순간에 분류까지 하라고 요구하는 셈이라, 결국 아무것도 채워지지 않은 달력만 남는다.
- **타깃**: 밤에 하루를 짧게 적어두는 사람. 가계부 앱은 무거워서 그만뒀고, 다이어리는 예쁘게 쓰려다 포기했다. "쫄면에 족발 먹음. 설거지하고 도서관 감. 투썸에서 쿠폰쓰고 차액 600원 결제"처럼 **한 문장에 활동과 지출이 섞여 있는 방식으로 쓴다.** 이 사람은 자기 기록을 다시 정리할 생각이 없다.

## 3. 핵심 시나리오

1. **진입**: 밤 11시, 타임라인 탭을 연다 (앱의 기본 화면)
2. **입력**: 평소처럼 한 줄 적는다 — "밥먹고~교회다녀오고~다이소갔다가 엄마아빠 만나고~베라까지 먹고 집에 도착함"
3. **AI 처리**: 저장 직후, 그날 기록 전체를 읽고 활동 태그와 금액을 뽑아낸다 (사용자는 기다리지 않는다)
4. **결과**: 메모 아래에 작은 칩으로 `외식` `가족` 이 붙고, 먼슬리 달력의 오늘 칸이 채워진다
5. **다음 행동**: 아무것도 안 해도 된다. 칩이 틀렸으면 눌러서 지우거나 고친다 — 그게 다음 추출에 반영된다

## 4. 기능 범위

**Must** (3개)

1. **하루 기록 자동 구조화** — 그날 메모들을 읽고 활동 태그와 금액(수입/지출)을 뽑아 저장
2. **틀린 결과를 한 번의 탭으로 고치기** — 칩 삭제·추가, 금액 수정. 확인 절차 없이 즉시 반영
3. **기존 뷰에 그대로 반영** — 먼슬리 달력 칸과 하단 패널의 수입/지출이 자동으로 채워짐 (새 화면 없음)

**Won't** (이번에 안 함)

- 하루 요약문·회고 코멘트 생성
- 잘했다/못했다 평가, 조언, 응원 메시지
- "오늘 기록이 없어요" 같은 알림·리마인더·연속 기록 카운트
- 대화형 챗봇, 질문하기
- 음성 입력, 사진에서 영수증 읽기
- 여러 날·여러 주를 묶은 분석 리포트
- 예산 설정과 초과 경고
- 카테고리 자동 학습·개인화 (v1은 고정 태그 집합)
- 위클리 뷰 변경 (지금의 '리듬 보기' 그대로 둠)
- 한국어 외 언어
- 공유, 내보내기, 소셜 기능

## 5. 화면 구성

- **화면 1 · 타임라인 (기존, 기본 진입)**: 날짜 헤더 / 시간 + 메모 말풍선 목록 / **메모 아래 활동 태그 칩(신규)** / **금액이 인식된 메모의 우측 금액 배지(신규)** / 입력창 + 전송 버튼 / 하단 탭바
- **화면 2 · 칩 편집 (신규, 바텀시트)**: 메모 원문 한 줄 / 인식된 태그 칩(각 칩에 x) / 태그 추가 버튼 / 금액·수입지출 입력칸 / 닫기 — **저장 버튼 없음, 바꾸는 즉시 반영**
- **화면 3 · 먼슬리 (기존, 변경 없음)**: 달력 칸의 습관 점과 수입/지출이 자동 추출값으로 채워짐. UI는 그대로
- **화면 4 · 마이페이지 (기존, 한 줄 추가)**: '자동 정리' 토글 — 끄면 예전처럼 등록 키워드 방식으로만 동작

## 6. AI 동작 정의

**입력**

- 형식: 하루치 메모 배열 `[{ id, time: "HH:mm", text }]`
- 최대: 메모 100건 / 합계 4,000자. 초과 시 최근 100건만 전송
- 사용자 프로필, 과거 기록, 다른 날짜는 보내지 않음 (하루 단위로 닫힘)

**출력** (JSON 고정 — 구조화 출력 스키마로 강제)

```json
{
  "memos": [
    {
      "id": "메모 id (입력과 동일)",
      "tags": ["고정 태그 집합 중 0~3개"],
      "amount": 18900,
      "direction": "expense | income | null",
      "confidence": 0.0
    }
  ]
}
```

- `tags` 는 고정 집합에서만 선택: `식사` `외식` `카페` `쇼핑` `이동` `운동` `공부` `독서` `일` `집안일` `가족` `모임` `휴식` `건강` `취미`
- 확실하지 않으면 `tags: []`, `amount: null` — **빈 값이 틀린 값보다 낫다**
- `confidence < 0.6` 인 항목은 저장하되 화면에 표시하지 않음

**프롬프트 방향**

- 역할: 사용자가 쓴 하루 기록에서 활동과 금액만 뽑아내는 추출기. 해석하거나 요약하지 않는다.
- 톤: 출력은 JSON뿐. 사람에게 말을 거는 문장을 만들지 않는다.
- 금지: (1) 기록에 없는 내용을 추론해 채우지 말 것 — 애매하면 비운다. (2) 평가·조언·격려·감정 표현을 어떤 필드에도 넣지 말 것. (3) 원문을 고치거나 다듬지 말 것.

**실패 처리**

- API 오류·타임아웃·스키마 불일치 → **1회 재시도 후, 기존 키워드 매칭 방식으로 조용히 되돌아감**
- 사용자 화면에 에러 메시지·실패 배너·재시도 버튼을 띄우지 않는다. 칩이 안 붙을 뿐이고, 기록 자체는 언제나 정상 저장된다
- 연속 실패 시에만 마이페이지 '자동 정리' 항목 밑에 회색 한 줄: *"지금은 자동 정리가 쉬는 중이에요"*
- **이 앱의 설계 원칙: 사용자에게 부담을 주지 않는다.** 실패를 사용자의 할 일로 만들지 않는다

## 7. 기술 스택

**기존 (변경 없음)**

- 프론트: React 19 + Vite (단일 `src/App.jsx`), 순수 CSS
- 백엔드/DB: Supabase (Postgres + Auth + Realtime + RLS)
- 배포: Vercel (CLI 수동 배포, git remote 없음), `vercel.json` cron으로 Supabase keep-alive

**추가**

- AI 호출: **Vercel Serverless Function** (`api/extract.js`) — 브라우저에서 직접 부르지 않는다. API 키가 노출되기 때문
- SDK: `@anthropic-ai/sdk`
- 모델: `claude-opus-5`
  - `output_config: { effort: "low", format: { type: "json_schema", schema: … } }` — 구조화 출력으로 JSON 스키마를 강제 (프리필·정규식 파싱 불필요)
  - `max_tokens: 4000`, 스트리밍 없음
  - 비용이 문제가 되면 effort를 낮추는 것으로 먼저 대응하고, 그래도 부담이면 더 작은 모델로 내리는 건 사용자 판단
- 호출 시점: 메모 저장 후 debounce 5초 (한 번에 몰아서 1회 호출), 같은 날 재호출 시 이전 결과 덮어쓰기

## 8. 데이터

**Supabase `memos` 테이블에 컬럼 추가**

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `ai_tags` | `jsonb` | 추출된 태그 배열. 사용자가 고치면 이 값이 바뀜 |
| `ai_amount` | `integer` | 금액 (원). 없으면 null |
| `ai_direction` | `text` | `expense` / `income` / null |
| `ai_confidence` | `real` | 0~1 |
| `ai_edited` | `boolean` | 사용자가 손댔는지. true면 재추출이 덮어쓰지 않음 |
| `ai_extracted_at` | `timestamptz` | 마지막 추출 시각 |

**저장하지 않는 것**

- 프롬프트 원문, 모델 응답 원본, 추출 이력 로그
- 사용자별 학습 데이터 (v1은 개인화 없음)

**기존 데이터 취급**

- 메모 원문(`content`)은 절대 수정하지 않는다. AI 결과는 항상 별도 컬럼
- `settings.habit_keywords`(수동 등록 키워드)는 그대로 유지 — 자동 정리를 끄면 이쪽으로 되돌아감
- 과거 메모 일괄 재추출은 하지 않는다 (비용·놀람 방지). 사용자가 그 날짜를 열면 그때 처리

---

## 부록: 왜 이 AI 기능인가

양식에 AI 기능 항목이 있어 하나를 골라야 했고, **지금 앱에서 사람이 손으로 하고 있는 일** 중 가장 성가신 것을 택했습니다.

- 지금 습관 집계는 `settings.habit_keywords`에 등록한 단어를 **문자열 포함 검사**로 세는 방식입니다. "그림"을 등록해야 그림 기록이 잡히고, "드로잉"이라고 쓰면 안 잡힙니다.
- 가계부도 마찬가지로 사용자가 금액을 알아볼 수 있는 모양으로 적어줘야 합니다.
- 즉 **분류 부담이 이미 사용자에게 있고**, 이 앱의 설계 원칙("부담을 주지 않는다")과 정면으로 충돌합니다. AI를 여기에 쓰면 기능이 늘어나는 게 아니라 **할 일이 줄어듭니다.**

### 고려했지만 뺀 대안

| 대안 | 뺀 이유 |
|---|---|
| 하루 요약·회고 문장 생성 | 요약을 읽는 것 자체가 새로운 할 일. "오늘은 알찬 하루였네요" 류는 평가로 읽혀 부담을 준다 |
| 주간 리포트·패턴 분석 | 위클리는 "너 이번 주 왜 이랬어" 화면이 되기 쉬워 이미 의도적으로 피한 방향 |
| 대화형 질문 ("지난달 카페에 얼마 썼어?") | 기록 앱이 아니라 다른 제품이 된다. Must 3개 안에 들어갈 수 없음 |

이 선택이 과제 의도와 다르면 6번 섹션만 갈아끼우면 됩니다 — 나머지 섹션은 실제 앱 기준이라 그대로 씁니다.
